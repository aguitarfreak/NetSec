import java.util.ArrayList;
import java.util.Collection;
import java.util.HashMap;
import java.util.Map;
import org.apache.commons.collections.MultiHashMap;
import org.apache.commons.collections.MultiMap;

public class test {
	
	public static HashMap<String, ArrayList<String>> test = new HashMap<String, ArrayList<String>>();
	static MultiMap map = new MultiHashMap( );
	
	public static void main(String[] args) {
		/*String test = "1ffe";
		
		int value = 0;
		

		value = (int) ((Math.pow(16, 3)*Character.getNumericValue(test.charAt(0)) +
				(Math.pow(16, 2)*Character.getNumericValue(test.charAt(1)) +
				(Math.pow(16, 1)*Character.getNumericValue(test.charAt(2)) +
				(Math.pow(16, 0)*Character.getNumericValue(test.charAt(3)))))))*8;
		
		System.out.println(value);*/
		
		/*int x = -1234456;
		System.out.println(Integer.toString(x));*/
	}

}
